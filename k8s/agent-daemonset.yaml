apiVersion: v1
kind: ServiceAccount
metadata:
  name: KUBEWHISPER-agent
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: KUBEWHISPER-agent
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: KUBEWHISPER-agent
subjects:
  - kind: ServiceAccount
    name: KUBEWHISPER-agent
    namespace: default
roleRef:
  kind: ClusterRole
  name: KUBEWHISPER-agent
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: KUBEWHISPER-agent
  namespace: default
  labels:
    app: KUBEWHISPER-agent
spec:
  selector:
    matchLabels:
      app: KUBEWHISPER-agent
  template:
    metadata:
      labels:
        app: KUBEWHISPER-agent
    spec:
      serviceAccountName: KUBEWHISPER-agent
      containers:
        - name: agent
          image: KUBEWHISPER-agent:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: KUBEWHISPER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: KUBEWHISPER-secrets
                  key: KUBEWHISPER_API_KEY
            - name: BRAIN_URL
              value: http://KUBEWHISPER-brain.default.svc.cluster.local:8000/analyze
          resources:
            requests:
              memory: "16Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"