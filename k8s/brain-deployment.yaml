apiVersion: apps/v1
kind: Deployment
metadata:
  name: KUBEWHISPER-brain
  namespace: default
  labels:
    app: KUBEWHISPER-brain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: KUBEWHISPER-brain
  template:
    metadata:
      labels:
        app: KUBEWHISPER-brain
    spec:
      containers:
        - name: brain
          image: KUBEWHISPER-brain:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: KUBEWHISPER-secrets
                  key: GEMINI_API_KEY
            - name: KUBEWHISPER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: KUBEWHISPER-secrets
                  key: KUBEWHISPER_API_KEY
            - name: DB_PATH
              value: /data/db_storage
            - name: LIBRARIAN_CONFIG
              value: /config/sources.yaml
          volumeMounts:
            - name: db-storage
              mountPath: /data/db_storage
            - name: config-volume
              mountPath: /config
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: db-storage
          emptyDir: {}
        - name: config-volume
          configMap:
            name: KUBEWHISPER-config
---
apiVersion: v1
kind: Service
metadata:
  name: KUBEWHISPER-brain
  namespace: default
spec:
  selector:
    app: KUBEWHISPER-brain
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000