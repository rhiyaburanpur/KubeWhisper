apiVersion: v1
kind: Pod
metadata:
  name: scenario-11-fd-limit
  labels:
    scenario: "11"
    category: resource
spec:
  containers:
  - name: crasher
    image: busybox
    command: ["/bin/sh", "-c"]
    args: ["echo 'FATAL: too many open files (EMFILE)'; exit 1"]
