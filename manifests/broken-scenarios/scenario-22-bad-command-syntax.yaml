apiVersion: v1
kind: Pod
metadata:
  name: scenario-22-bad-cmd
  labels:
    scenario: "22"
    category: config
spec:
  containers:
  - name: crasher
    image: busybox
    command: ["/bin/sh"]
    args: ["-c", "echo 'starting'; nonexistent_binary --flag; exit $?"]
